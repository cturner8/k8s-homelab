---
namespace: miniflux

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd

generatorOptions:
  disableNameSuffixHash: true
  labels:
    app.kubernetes.io/managed-by: kustomize

configMapGenerator:
  - name: miniflux-conf
    files:
      - miniflux.conf

resources:
  - secret.yml
  - volume.yml
  - deployment.yml
  - service.yml
  - ingress.yml
