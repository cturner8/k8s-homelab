---
namespace: ntfy

generatorOptions:
  disableNameSuffixHash: true
  labels:
    app.kubernetes.io/managed-by: kustomize

configMapGenerator:
  - name: ntfy-conf
    files:
      - server.yml

resources:
  - namespace.yml
  - volume.yml
  - deployment.yml
  - service.yml
  - ingress.yml
