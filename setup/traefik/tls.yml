---
apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default

spec:
  certificates:
    - secretName: local-gd-tls
    - secretName: dev-cturner-xyz-tls
  defaultCertificate:
    secretName: dev-cturner-xyz-tls
